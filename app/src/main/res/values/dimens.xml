<resources>

    <!-- 主要是headview 标题大小 -->
    <dimen name="text_title">20sp</dimen>
    <dimen name="text_middle">18sp</dimen>

    <!-- 比一般文字大一点的 -->
    <dimen name="text_normal_big">18sp</dimen>

    <!-- 一般文字大小 -->
    <dimen name="text_normal">16sp</dimen>

    <!-- 小文字大小 -->
    <dimen name="text_small">14sp</dimen>

    <!-- 更小文字(多用于灰色日期) -->
    <dimen name="text_litte">12sp</dimen>

    <!-- 侧边栏距离右边的偏移 -->
    <dimen name="slidingmenu_offset">45dip</dimen>

    <!-- 常见button的边角幅度 -->
    <dimen name="btn_root_radius">3dp</dimen>

    <!-- 主Tab字体的大小 -->
    <dimen name="bottom_tab_font_size">14sp</dimen>

    <!-- tab内容离上边的位置 -->
    <dimen name="bottom_tab_padding_up">2dip</dimen>

    <!-- tab上图片的位置 -->
    <dimen name="bottom_tab_padding_drawable">2dip</dimen>

    <!-- head view 的高度 -->
    <dimen name="head_view_height">45dip</dimen>

    <!-- 评论增加图片的大小 -->
    <dimen name="reviews_picture_size">65dip</dimen>
    <dimen name="fab_size_normal">56dp</dimen>
    <dimen name="fab_size_mini">40dp</dimen>
    <dimen name="fab_shadow_size">8dp</dimen>
    <dimen name="fab_mini_shadow_size">6dp</dimen>
    <dimen name="MaxImageHeight">350dip</dimen>
    <dimen name="MinImageHeight">210dip</dimen>
</resources>